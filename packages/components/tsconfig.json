{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@c/*": ["src/*"]
    },
    "types": ["vite/client"],
    // 子项目特定的配置，会覆盖主项目的配置
    "outDir": "./dist"
  },
  "include": ["src/**/*.ts", "src/**/*.vue"],
  "exclude": ["node_modules"]
}
