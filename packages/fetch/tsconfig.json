{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "paths": {
      "@f/*": ["./src/*"]
    },
    // 子项目特定的配置，会覆盖主项目的配置
    "outDir": "./dist"
  },

  "include": ["src/**/*.ts"],
  "exclude": ["node_modules"]
}
